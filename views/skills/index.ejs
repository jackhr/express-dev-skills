<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Dev Skills</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
  <a href="/">Go back</a>
  <br>
  <h1>Skills</h1>
  <!-- GET RID OF THE LIST BECUASE IT'S IMPOSSIBLE TO CENTER IT -->
  <ul>
    <% skills.forEach(function(s) { %>
      <li>
        <form action="/skills/<%= s.id %>?_method=DELETE" class="delete-form" method="POST">
          <button class="destroy" type="submit">DESTROY</button>
        </form>
        <a href="/skills/<%= s.id %>"><%= s.skill %></a>
      </li>
    <% }); %>
  </ul>
  <p>Wanna pop a <a href="/skills/new">new skill</a> into the mix?</p>
</body>
</html>